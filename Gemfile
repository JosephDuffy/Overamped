source "https://rubygems.org"

gem "fastlane"
gem "abbrev" # Needed from ruby 3.4.0
gem "logger" # Will be needed from ruby 3.5.0, if fastlane doesn't add this.
gem "ostruct" # Will be needed from ruby 3.5.0, if fastlane doesn't add this.
